apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
        - name: flask-deployment
          image: MiaAcr.azurecr.io/:732fc1b167abef6e8565e0677d895e18389bcac0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 2000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
